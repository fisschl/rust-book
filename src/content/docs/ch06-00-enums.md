---
title: "枚举和模式匹配"
---

在本章中，我们将介绍枚举，也称为 *enums*。枚举允许你通过列举其可能的变体来定义一种类型。首先，我们将定义并使用一个枚举来展示枚举如何编码含义以及数据。接下来，我们将探讨一个特别有用的枚举，称为 `Option`，它表示一个值可能是某些内容，也可能是没有。然后，我们将了解 `match` 表达式中的模式匹配如何使针对枚举的不同值运行不同代码变得容易。最后，我们将介绍 `if let` 构造是处理代码中枚举的另一种方便且简洁的惯用写法。
